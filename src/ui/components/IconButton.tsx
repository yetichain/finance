import styled from 'styled-components'

const IconButton = styled.button`
  display: inline-flex;
  align-items: center;
  background: none;
  border: none;
  cursor: pointer;
`

export default IconButton
